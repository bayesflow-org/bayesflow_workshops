name: bf
channels:
  - conda-forge
  - defaults
dependencies:
  - python >=3.10,<3.13
  - pip
  - pip:
      - bayesflow
      - jax
      - jupyter
      - jupyterlab
      - ipython
      - ipykernel
